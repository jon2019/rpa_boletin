# Test outputs

Este directorio concentra artefactos generados por scripts manuales y diagnósticos:

- logs de pruebas manuales
- JSON de auditoría
- screenshots
- HTML de debug y previews locales

`logs/` queda reservado solo para el log operativo principal:

- `boletin_YYYY-MM-DD.log`
